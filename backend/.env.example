# Copy to .env for local dev. On Render, these are set as environment variables
# (DATABASE_URL is auto-populated if you use the render.yaml blueprint).
DATABASE_URL=postgres://user:password@localhost:5432/shruhi_portal
JWT_SECRET=change-this-to-a-long-random-string
PORT=4000
